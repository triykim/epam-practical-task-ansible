---
- name: Print Network intefraces
  hosts: managed_nodes
  become: true
  vars:
    required_packages:
      - curl
      - lsof
      - mc
      - nano
      - tar
      - unzip
      - vim
      - zip
    disable_selinux: true
  roles:
    - common
